apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

resources:
  - serviceaccount.yaml
  - configmap.yaml
  - cronjob-audit.yaml
  - cronjob-enforce.yaml

# Uncomment to include the secret manifest directly, or provision secrets via
# HashiCorp Vault, Sealed Secrets, or AWS/GCP External Secrets Operator.
# resources:
#   - secret-template.yaml
